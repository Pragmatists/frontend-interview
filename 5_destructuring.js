
// get the head (first) and tail (everything except first element) in the array;

const array = [ 1, 2, 3, 4, 5, 6, 7, 8 ];

// rewrite using destructuring

console.log(area({ width: 5 }))

function area(rect) {
  const width = rect.width || 0;
  const height = rect.height || width;
  return width * height;

}
